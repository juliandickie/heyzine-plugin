# dev-docs

Tracked, dated, additive logs - the API audit (account identifiers redacted) and the release
checklist. Session handoffs, smoke records, build ledgers and link mappings for a specific
account live outside the repo (the plugin author keeps them under the gitignored `docs/`).
